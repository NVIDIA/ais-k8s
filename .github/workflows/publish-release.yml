name: Publish ais-k8s release 

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'version for operator test release'
        required: true
        type: string
        default: 'v0.97'

env:
  AISOPERATOR_IMAGE: 'aistorage/ais-operator'
  VERSION: ${{ inputs.version }}

jobs:
  release:
    runs-on: self-hosted
    steps:
    
    - name: Checkout Repo
      uses: actions/checkout@v2
    - uses: actions/setup-go@v4
      with:
        go-version: '>=1.21.0'

    - name: make aisoperator image
      run: |
        pushd $GITHUB_WORKSPACE/operator
        IMG="${{ env.AISOPERATOR_IMAGE }}:${{ env.VERSION }}" make docker-build
        popd
  
    - name: make operator yaml file
      run: |
        pushd $GITHUB_WORKSPACE/operator
        make kustomize
        VERSION="${{ env.VERSION }}" IMG="${{ env.AISOPERATOR_IMAGE }}:${{ env.VERSION }}" make bundle-manifests
        popd

    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        files: operator/dist/*
